import { requireUser } from '@/src/lib/auth';import { supabaseFetch } from '@/src/lib/supabase/server';import { redirect } from 'next/navigation';import OnboardingForm from './form';
export const dynamic='force-dynamic';export default async function Page(){const user=await requireUser();const memberships=await supabaseFetch(`/rest/v1/organization_members?user_id=eq.${user.id}&select=organization_id&limit=1`);if(memberships?.length)redirect('/dashboard');return <main className="mx-auto max-w-xl p-6"><h1 className="text-3xl font-bold">أنشئ مساحتك في مَدار</h1><p className="mt-3 text-slate-300">لن يمنحك امتلاك مساحة أي صلاحية لإدارة منصة مَدار.</p><OnboardingForm/></main>}
