'use server';
import{revalidatePath}from'next/cache';import{requireAdmin,requireUser}from'@/src/lib/auth';import{supabaseFetch}from'@/src/lib/supabase/server';import{removeLocalPaymentProof,uploadLocalPaymentProof}from'@/src/lib/local-payments';
export type OrderState={error?:string;success?:string;orderId?:string};
const messages:Record<string,string>={INVALID_ITEMS:'السلة فارغة أو تحتوي عناصر كثيرة.',INVALID_QUANTITY:'الكمية المختارة غير صالحة.',PRODUCT_UNAVAILABLE:'أحد المنتجات لم يعد متاحًا.',SERVICE_UNAVAILABLE:'إحدى الخدمات لم تعد متاحة.',MIXED_CURRENCY_CART:'لا يمكن جمع عناصر بعملات مختلفة في طلب واحد. أكمل كل عملة في طلب مستقل.',INVALID_COUPON:'رمز الخصم غير صالح أو منتهي.',COUPON_MINIMUM:'لم يصل الطلب إلى الحد الأدنى للكوبون.',COUPON_EXHAUSTED:'اكتمل عدد مرات استخدام هذا الكوبون.',COUPON_USER_LIMIT:'سبق أن استخدمت هذا الكوبون بالحد المسموح.',PROFILE_REQUIRED:'أكمل ملفك الشخصي أولًا.',PAYMENT_REFERENCE_REQUIRED:'رقم العملية إلزامي.',ORDER_NOT_PAYABLE:'لا يمكن إرسال دفع لهذا الطلب الآن.',ORDER_NOT_REVIEWABLE:'تمت مراجعة هذا الطلب مسبقًا.',PRODUCT_FILE_REQUIRED:'ملف المنتج غير جاهز للتسليم.'};
function friendly(error:unknown,fallback:string){const value=error instanceof Error?error.message:'';for(const[k,m]of Object.entries(messages))if(value.includes(k))return m;return value&&/^[\u0600-\u06ff]/.test(value)?value:fallback}
export async function createOrder(_previous:OrderState,form:FormData):Promise<OrderState>{try{await requireUser();const items=JSON.parse(String(form.get('items')||'[]'));if(!Array.isArray(items))throw new Error('INVALID_ITEMS');const orderId=await supabaseFetch('/rest/v1/rpc/create_customer_order',{method:'POST',body:JSON.stringify({p_items:items.map((x:any)=>({id:String(x.id),type:x.type,quantity:Number(x.quantity),requirements:String(x.requirements||'')})),p_coupon_code:String(form.get('coupon')||'')||null,p_customer_phone:String(form.get('phone')||'')||null,p_customer_notes:String(form.get('notes')||'')||null})});revalidatePath('/account/orders');return{success:'تم إنشاء طلبك بنجاح.',orderId:String(orderId)}}catch(error){return{error:friendly(error,'تعذر إنشاء الطلب.')}}}
export async function submitPaymentProof(_previous:OrderState,form:FormData):Promise<OrderState>{let proof:Awaited<ReturnType<typeof uploadLocalPaymentProof>>|undefined;try{const user=await requireUser(),orderId=String(form.get('order_id')||''),reference=String(form.get('payment_reference')||'').trim();if(!/^[0-9a-f-]{36}$/i.test(orderId))throw new Error('الطلب غير صالح.');if(reference.length<3||reference.length>120)throw new Error('PAYMENT_REFERENCE_REQUIRED');const file=form.get('proof');if(file instanceof File&&file.size){proof=await uploadLocalPaymentProof(file,user.id,`orders/${orderId}`);await supabaseFetch('/rest/v1/payment_proofs',{method:'POST',body:JSON.stringify({order_id:orderId,user_id:user.id,storage_path:proof.storagePath,original_filename:proof.originalFilename,mime_type:proof.mimeType,file_size:proof.fileSize})})}await supabaseFetch('/rest/v1/rpc/submit_order_payment',{method:'POST',body:JSON.stringify({p_order_id:orderId,p_payment_reference:reference})});revalidatePath(`/account/orders/${orderId}`);revalidatePath('/account/orders');return{success:'تم إرسال رقم العملية للمراجعة.',orderId}}catch(error){if(proof)await removeLocalPaymentProof(proof.storagePath);return{error:friendly(error,'تعذر إرسال بيانات التحويل.')}}}
export async function reviewOrderPayment(form:FormData){try{await requireAdmin();const id=String(form.get('order_id')),approved=form.get('decision')==='approve',note=String(form.get('note')||'');await supabaseFetch('/rest/v1/rpc/review_order_payment',{method:'POST',body:JSON.stringify({p_order_id:id,p_approved:approved,p_note:note||null})});for(const path of['/admin/orders',`/admin/orders/${id}`,'/admin','/admin/reports','/account/purchases','/account/notifications'])revalidatePath(path);return{success:approved?'تم اعتماد الدفع وإنشاء الاستحقاق.':'تم رفض الدفع.'}}catch(error){return{error:friendly(error,'تعذر مراجعة الدفع.')}}}
export async function updateOrderFulfillment(form:FormData){try{await requireAdmin();const id=String(form.get('order_id')),status=String(form.get('status')),note=String(form.get('note')||'');await supabaseFetch('/rest/v1/rpc/update_order_fulfillment',{method:'POST',body:JSON.stringify({p_order_id:id,p_status:status,p_note:note||null})});revalidatePath('/admin/orders');revalidatePath(`/admin/orders/${id}`);return{success:'تم تحديث حالة الطلب.'}}catch(error){return{error:friendly(error,'تعذر تحديث الطلب.')}}}
export async function saveCoupon(_previous:OrderState,form:FormData):Promise<OrderState>{try{const actor=await requireAdmin(),id=String(form.get('id')||''),code=String(form.get('code')||'').trim().toUpperCase(),discountType=String(form.get('discount_type')),discountValue=Number(form.get('discount_value')),minimumOrder=Number(form.get('minimum_order')||0);if(!/^[A-Z0-9_-]{3,30}$/.test(code))throw new Error('رمز الكوبون غير صالح.');if(!['percent','fixed'].includes(discountType)||!Number.isFinite(discountValue)||discountValue<=0||(discountType==='percent'&&discountValue>100))throw new Error('قيمة الخصم غير صالحة.');const payload={code,description:String(form.get('description')||'')||null,discount_type:discountType,discount_value:discountValue,minimum_order:minimumOrder,usage_limit:form.get('usage_limit')?Number(form.get('usage_limit')):null,per_user_limit:Number(form.get('per_user_limit')||1),starts_at:form.get('starts_at')?new Date(String(form.get('starts_at'))).toISOString():null,ends_at:form.get('ends_at')?new Date(String(form.get('ends_at'))).toISOString():null,is_active:form.get('is_active')==='on'};await supabaseFetch(id?`/rest/v1/coupons?id=eq.${encodeURIComponent(id)}`:'/rest/v1/coupons',{method:id?'PATCH':'POST',body:JSON.stringify(id?payload:{...payload,created_by:actor.id})});revalidatePath('/admin/coupons');return{success:'تم حفظ الكوبون.'}}catch(error){return{error:friendly(error,'تعذر حفظ الكوبون.')}}}
export async function markNotificationsRead(){const user=await requireUser();await supabaseFetch(`/rest/v1/notifications?user_id=eq.${user.id}&read_at=is.null`,{method:'PATCH',body:JSON.stringify({read_at:new Date().toISOString()})});revalidatePath('/account/notifications')}
