import { requireAdmin } from '@/src/lib/auth';import { supabaseFetch } from '@/src/lib/supabase/server';import { EntityForm } from '@/components/admin/EntityForm';import { MediaManager } from '@/components/admin/MediaManager';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{id:string}>}){await requireAdmin();let service;try{service=(await supabaseFetch(`/rest/v1/services?id=eq.${encodeURIComponent((await params).id)}&select=*`))?.[0];}catch{}return <main className="mx-auto max-w-3xl p-6"><h1>تعديل خدمة</h1>{service?<><EntityForm kind="service" initial={service}/><MediaManager entity="service" id={service.id}/></>:<p>الخدمة غير موجودة.</p>}</main>}
