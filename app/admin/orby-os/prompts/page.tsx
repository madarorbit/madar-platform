import {requireSuperAdmin} from '@/src/lib/auth';import {supabaseFetch} from '@/src/lib/supabase/server';import OrbyOsSubnav from '@/components/admin/OrbyOsSubnav';import {publishPromptVersion} from '../actions';
export const dynamic='force-dynamic';export const metadata={title:'Prompts | ORBY OS'};type Prompt={id:string;key:string;domain:string;version:number;status:string;checksum:string;created_at:string};const field='w-full rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm text-white outline-none focus:border-violet-300/40';
export default async function Page(){await requireSuperAdmin();const prompts=await supabaseFetch('/rest/v1/orby_prompt_versions?select=id,key,domain,version,status,checksum,created_at&order=created_at.desc&limit=100') as Prompt[];return <main className="mx-auto max-w-[1300px] p-5 py-8 sm:p-8"><p className="font-bold text-violet-200">ORBY OS · Prompt Registry</p><h1 className="mt-2 text-3xl font-black">تعليمات النظام وإصداراتها</h1><p className="mt-3 text-slate-400">لا تُعدّل تعليمات أوربي داخل الكود؛ كل تغيير يُحفظ كإصدار مستقل قابل للتقييم والرجوع.</p><OrbyOsSubnav/><section className="mt-8 grid gap-6 lg:grid-cols-2"><form action={publishPromptVersion} className="rounded-3xl border border-white/10 bg-white/[.02] p-6"><h2 className="text-xl font-black">إصدار Prompt جديد</h2><div className="mt-5 grid gap-3 sm:grid-cols-2"><label>المفتاح<input name="key" required placeholder="core.system-policy" className={field}/></label><label>المجال<select name="domain" className={field}><option value="core">Core</option><option value="business">Business</option><option value="store">Store</option><option value="finance">Finance</option><option value="student">Student</option></select></label></div><label className="mt-3 block">الحالة<select name="status" className={field}><option value="testing">Testing</option><option value="draft">Draft</option><option value="canary">Canary</option><option value="active">Active</option></select></label><label className="mt-3 block">المحتوى<textarea name="content" required rows={16} className={field}/></label><button className="md-button md-button-primary mt-5" type="submit">حفظ الإصدار</button></form><section className="rounded-3xl border border-white/10 bg-white/[.02] p-6"><h2 className="text-xl font-black">سجل الإصدارات</h2><div className="mt-5 space-y-3">{prompts.length?prompts.map(item=><div key={item.id} className="rounded-2xl border border-white/10 p-4"><div className="flex justify-between gap-3"><strong>{item.key}</strong><span className="md-badge md-badge-brand">v{item.version} · {item.status}</span></div><p className="mt-2 text-xs text-slate-500">{item.domain} · checksum {item.checksum.slice(0,12)}…</p></div>):<p className="text-sm text-slate-500">لا توجد إصدارات Prompt بعد.</p>}</div></section></section></main>}
