import { notFound } from 'next/navigation';import PageShell from '@/components/ui/PageShell';import { Section } from '@/components/ui/Section';import { supabaseFetch } from '@/src/lib/supabase/server';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{slug:string}>}){let service;try{service=(await supabaseFetch(`/rest/v1/services?slug=eq.${encodeURIComponent((await params).slug)}&status=eq.published&select=*`))?.[0];}catch{throw new Error('تعذر تحميل الخدمة.');}if(!service)notFound();return <PageShell><Section><p className="text-[#00C292]">خدمة مَدار</p><h1 className="mt-3 text-4xl font-bold">{service.name}</h1><p className="mt-6 max-w-3xl text-lg text-slate-300">{service.long_description||service.short_description}</p><p className="mt-6 text-2xl">تبدأ من {service.price_from} {service.currency}</p></Section></PageShell>}
