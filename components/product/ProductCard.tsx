import Link from 'next/link';import type{Product}from '@/src/data/products';import{formatPrice}from '@/src/data/products';import{Icon}from '@/components/ui/Icons';
function graphic(product:Product){const text=`${product.category} ${product.title}`;if(/ذكاء|AI/i.test(text))return 'sparkles';if(/أتمت|عمليات/i.test(text))return'automation';if(/بيانات|مبيعات|Sheets/i.test(text))return'chart';return'layers'}
export default function ProductCard({product}:{product:Product}){return <article className="group flex h-full flex-col overflow-hidden rounded-[1.75rem] border border-slate-200 bg-white shadow-sm transition duration-300 hover:-translate-y-1 hover:border-violet-200 hover:shadow-xl"><div className="relative aspect-[16/10] overflow-hidden bg-[#0B1020]">{product.thumbnailUrl?<div className="absolute inset-0 bg-cover bg-center transition duration-500 group-hover:scale-105" style={{backgroundImage:`url("${product.thumbnailUrl.replace(/["\\]/g,'')}")`}} role="img" aria-label={`صورة ${product.title}`}/>:<div className="absolute inset-0 grid place-items-center bg-[radial-gradient(circle_at_30%_25%,rgba(108,59,255,.55),transparent_32%),radial-gradient(circle_at_75%_70%,rgba(0,194,168,.45),transparent_36%)]"><div className="grid h-24 w-24 place-items-center rounded-[1.75rem] border border-white/15 bg-white/10 text-white shadow-2xl backdrop-blur"><Icon name={graphic(product)} className="h-12 w-12"/></div></div>}<span className="absolute right-4 top-4 rounded-full border border-white/15 bg-black/40 px-3 py-1.5 text-xs font-bold text-white backdrop-blur">منتج رقمي</span></div><div className="flex flex-1 flex-col p-6"><p className="text-sm font-bold text-[#5B2AE8]">{product.category}</p><h3 className="mt-2 text-2xl font-black leading-8 text-slate-950">{product.title}</h3><p className="mt-3 flex-1 leading-7 text-slate-600">{product.description}</p><div className="mt-6 flex items-center justify-between gap-4 border-t border-slate-100 pt-5"><span className="text-xl font-black text-[#008B76]">{formatPrice(product)}</span><Link href={`/products/${product.slug}`} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-slate-950 px-4 py-2 font-bold text-white transition hover:bg-[#5B2AE8]">التفاصيل <Icon name="arrow" className="h-4 w-4 rotate-180"/></Link></div></div></article>}
