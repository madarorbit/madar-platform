'use client';import{useActionState}from'react';import{saveCurrency,saveExchangeRate,saveStorePaymentMethod,saveReportingCurrency,deleteStorePaymentMethod,type FinanceState}from'@/app/actions/store-finance';const initial:FinanceState={};function State({s}:{s:FinanceState}){return <>{s.error&&<p className="text-sm text-red-200">{s.error}</p>}{s.success&&<p className="text-sm text-emerald-200">{s.success}</p>}</>}
function MethodForm({method,currencies}:{method?:any;currencies:any[]}){const[s,a,p]=useActionState(saveStorePaymentMethod,initial),accepted=(method?.payment_method_currencies||[]).map((x:any)=>x.currency_code);return <form action={a} className="rounded-2xl border border-white/10 p-4 grid gap-3 md:grid-cols-2">{method&&<input type="hidden" name="id" value={method.id}/>}<input name="code" required defaultValue={method?.code||''} placeholder="METHOD_CODE" className="field" dir="ltr"/><input name="name" required defaultValue={method?.name||''} placeholder="اسم الوسيلة" className="field"/><select name="method_type" defaultValue={method?.method_type||'wallet'} className="field"><option value="wallet">محفظة</option><option value="bank">حساب بنكي</option></select><select name="legacy_currency" defaultValue={method?.currency||currencies[0]?.code} className="field">{currencies.map(c=><option key={c.code}>{c.code}</option>)}</select><input name="account_name" defaultValue={method?.account_name||''} placeholder="صاحب الحساب" className="field"/><input name="account_identifier" defaultValue={method?.account_identifier||''} placeholder="رقم الحساب/المحفظة" className="field" dir="ltr"/><input name="logo_path" defaultValue={method?.logo_path||''} placeholder="مسار/رابط الشعار" className="field" dir="ltr"/><input name="sort_order" type="number" defaultValue={method?.sort_order||0} className="field"/><textarea name="instructions" defaultValue={method?.instructions||''} placeholder="تعليمات الدفع" className="field md:col-span-2"/><label className="flex items-center gap-2"><input name="currency_agnostic" type="checkbox" defaultChecked={method?method.currency_agnostic:true}/>تقبل كل العملات المفعلة</label><label className="flex items-center gap-2"><input name="is_active" type="checkbox" defaultChecked={method?method.is_active:true}/>مفعلة</label><fieldset className="md:col-span-2"><legend className="text-sm font-bold">العملات المقبولة عند تقييد الوسيلة</legend><div className="mt-2 flex flex-wrap gap-2">{currencies.map(c=><label key={c.code} className="md-badge"><input name="accepted_currencies" value={c.code} type="checkbox" defaultChecked={accepted.includes(c.code)}/> {c.code}</label>)}</div></fieldset><button disabled={p} className="md-button md-button-primary">{method?'حفظ التعديل':'إضافة وسيلة'}</button>{method&&<button formAction={deleteStorePaymentMethod} name="id" value={method.id} className="md-button md-button-danger">حذف/تعطيل</button>}<State s={s}/></form>}
export default function StoreFinanceManager({currencies,rates,methods,reportingCurrency}:{currencies:any[];rates:any[];methods:any[];reportingCurrency:string}){const[cs,ca,cp]=useActionState(saveCurrency,initial),[rs,ra,rp]=useActionState(saveExchangeRate,initial),[ss,sa,sp]=useActionState(saveReportingCurrency,initial);return <div className="grid gap-7"><section className="md-panel"><h2 className="text-xl font-black">العملات</h2><form action={ca} className="mt-4 grid gap-3 md:grid-cols-5"><input required name="code" placeholder="AED" className="field" dir="ltr"/><input required name="name" placeholder="اسم العملة" className="field"/><input required name="symbol" placeholder="الرمز" className="field"/><input required name="decimal_places" type="number" min="0" max="8" defaultValue="2" className="field"/><label className="flex items-center gap-2"><input name="is_active" type="checkbox" defaultChecked/>مفعلة</label><button disabled={cp} className="md-button md-button-primary">إضافة/تحديث</button><State s={cs}/></form><div className="mt-5 grid gap-2 sm:grid-cols-2">{currencies.map(c=><div key={c.code} className="rounded-xl border border-white/10 p-3"><strong>{c.code} · {c.name}</strong><p className="text-xs text-slate-400">{c.symbol} · {c.decimal_places} منازل · {c.is_active?'Active':'Inactive'}</p></div>)}</div></section><section className="md-panel"><h2 className="text-xl font-black">أسعار الصرف</h2><p className="mt-1 text-sm text-slate-400">1 من العملة الأساسية = rate من العملة المقابلة.</p><form action={ra} className="mt-4 grid gap-3 md:grid-cols-5"><select name="base_currency" className="field">{currencies.map(c=><option key={c.code}>{c.code}</option>)}</select><select name="quote_currency" className="field">{currencies.map(c=><option key={c.code}>{c.code}</option>)}</select><input required name="rate" type="number" step="any" min="0" className="field"/><select name="status" className="field"><option value="active">Active</option><option value="inactive">Inactive</option></select><button disabled={rp} className="md-button md-button-primary">إضافة/تحديث السعر</button><State s={rs}/></form><div className="mt-5 grid gap-2">{rates.map(r=><div key={r.id} className="rounded-xl border border-white/10 p-3">1 {r.base_currency} = <strong>{r.rate}</strong> {r.quote_currency} · {r.status}</div>)}</div></section><section className="md-panel"><h2 className="text-xl font-black">وسائل الدفع</h2><p className="mt-1 text-sm text-slate-400">كل وسيلة تقبل كل العملات افتراضيًا؛ فعّل القيود فقط إذا كانت الوسيلة نفسها لا تقبل عملة معينة.</p><div className="mt-5 grid gap-4 lg:grid-cols-2">{methods.map(m=><MethodForm key={m.id} method={m} currencies={currencies}/>) }<MethodForm currencies={currencies}/></div></section><section className="md-panel"><h2 className="text-xl font-black">عملة التقارير المرجعية</h2><form action={sa} className="mt-4 flex flex-wrap gap-3"><select name="currency" defaultValue={reportingCurrency} className="field max-w-xs">{currencies.map(c=><option key={c.code}>{c.code}</option>)}</select><button disabled={sp} className="md-button md-button-primary">حفظ</button><State s={ss}/></form><p className="mt-3 text-xs text-slate-400">للتحليل التقريبي فقط، ولا تفرض على العميل الدفع بها.</p></section></div>}
