import {randomUUID} from 'node:crypto';
import type {
 OrbyEventBus,OrbyKernelRequest,OrbyKernelResponse,OrbyKernelStreamEvent,OrbyLogger,OrbyProviderCapability,OrbyProviderRequest,
 OrbyRoutingAttempt,OrbyRuntimeConfiguration,OrbySession,
} from './core/contracts';
import {OrbyError,normalizeOrbyError} from './core/errors';
import {
 OrbyConfigurationManager,OrbyContextEngine,OrbyResponseManager,OrbyRoutingEngine,OrbySessionManager,DefaultOrbyPromptCompiler,orbyMessage,
} from './core/runtime';
import type {OrbyIntelligenceRouter,OrbyIntelligenceRoutingDecision} from './intelligence/router';

export type OrbyKernelDependencies={configuration:OrbyConfigurationManager;sessions:OrbySessionManager;context:OrbyContextEngine;routing:OrbyRoutingEngine;intelligenceRouter?:OrbyIntelligenceRouter;events:OrbyEventBus;logger:OrbyLogger;promptCompiler?:DefaultOrbyPromptCompiler;responseManager?:OrbyResponseManager};

function validateRequest(request:OrbyKernelRequest){if(!request.identity.organizationId.trim()||!request.identity.userId.trim())throw new OrbyError('هوية المستخدم ومساحة العمل مطلوبة لتشغيل أوربي.','VALIDATION_ERROR');if(!request.message.trim())throw new OrbyError('رسالة أوربي لا يمكن أن تكون فارغة.','VALIDATION_ERROR');if(request.message.length>100_000)throw new OrbyError('رسالة أوربي أكبر من الحد المسموح.','VALIDATION_ERROR');}
function providerRequest(requestId:string,model:string,messages:OrbyProviderRequest['messages'],configuration:OrbyRuntimeConfiguration,request:OrbyKernelRequest):OrbyProviderRequest{return {requestId,model,messages,signal:request.signal,options:{timeoutMs:configuration.requestTimeoutMs,temperature:0.2,maxOutputTokens:2048,responseFormat:'text',metadata:request.metadata}};}
function fallbackDecision(request:OrbyKernelRequest,configuration:OrbyRuntimeConfiguration):OrbyIntelligenceRoutingDecision{const sensitivity=request.metadata?.sensitivity;return{mode:'static',preferredModelId:request.preferredModelId||configuration.defaultModelId,intent:typeof request.metadata?.intent==='string'?request.metadata.intent:'conversation',sensitivity:sensitivity==='sensitive'||sensitivity==='restricted'?sensitivity:'normal',complexity:0,candidates:[]};}
function routingMetadata(decision:OrbyIntelligenceRoutingDecision){return{routingMode:decision.mode,routingIntent:decision.intent,routingSensitivity:decision.sensitivity,routingComplexity:decision.complexity};}

export class OrbyKernel {
 private readonly promptCompiler:DefaultOrbyPromptCompiler;private readonly responseManager:OrbyResponseManager;
 constructor(private readonly dependencies:OrbyKernelDependencies){this.promptCompiler=dependencies.promptCompiler||new DefaultOrbyPromptCompiler();this.responseManager=dependencies.responseManager||new OrbyResponseManager();}
 private async prepare(request:OrbyKernelRequest,requestId:string){validateRequest(request);const configuration=await this.dependencies.configuration.resolve({organizationId:request.identity.organizationId});if(!configuration.enabled)throw new OrbyError('نواة أوربي مبنية لكنها غير مفعلة لهذه المساحة.','ORBY_DISABLED',false,{organizationId:request.identity.organizationId});const session=await this.dependencies.sessions.resolve({sessionId:request.sessionId,organizationId:request.identity.organizationId,userId:request.identity.userId,workspaceId:request.identity.workspaceId,ttlSeconds:configuration.sessionTtlSeconds});await this.dependencies.events.emit('request.started',{requestId,organizationId:request.identity.organizationId,userId:request.identity.userId,sessionId:session.id});const history=await this.dependencies.sessions.history(session.id,configuration.sessionHistoryLimit);const historyCharacters=history.reduce((sum,item)=>sum+item.content.length,0),contextBudget=Math.max(0,configuration.maxContextCharacters-historyCharacters-request.message.length-configuration.systemPolicies.join('').length-1000);const context=await this.dependencies.context.build({identity:request.identity,sessionId:session.id,message:request.message,metadata:request.metadata,signal:request.signal},contextBudget);const prompt=this.promptCompiler.compile({systemPolicies:configuration.systemPolicies,context,history,message:request.message,maxCharacters:configuration.maxContextCharacters});return {configuration,session,prompt};}
 private decide(request:OrbyKernelRequest,configuration:OrbyRuntimeConfiguration,requiredCapabilities:readonly OrbyProviderCapability[]){return this.dependencies.intelligenceRouter?.decide({request,configuration,requiredCapabilities})||fallbackDecision(request,configuration);}
 async execute(request:OrbyKernelRequest):Promise<OrbyKernelResponse>{const requestId=request.requestId||randomUUID(),started=Date.now();let session:OrbySession|undefined;try{const prepared=await this.prepare(request,requestId);session=prepared.session;const requiredCapabilities=request.requiredCapabilities||['text'],decision=this.decide(request,prepared.configuration,requiredCapabilities),preferredModelId=decision.preferredModelId||prepared.configuration.defaultModelId;const routed=await this.dependencies.routing.generate(providerRequest(requestId,preferredModelId||'auto',prepared.prompt.messages,prepared.configuration,request),{preferredModelId,allowedProviderIds:prepared.configuration.allowedProviderIds,allowedModelIds:prepared.configuration.allowedModelIds,requiredCapabilities,maxAttempts:prepared.configuration.maxAttempts,retryBaseDelayMs:prepared.configuration.retryBaseDelayMs});const normalized=this.responseManager.normalize(routed.response),durationMs=Date.now()-started,routeMeta=routingMetadata(decision);await this.dependencies.sessions.append(session,[orbyMessage('user',request.message,request.metadata),orbyMessage('assistant',normalized.text,{providerId:routed.selection.provider.id,modelId:routed.selection.model.id,requestId,...routeMeta})]);await this.dependencies.events.emit('request.completed',{requestId,organizationId:request.identity.organizationId,userId:request.identity.userId,sessionId:session.id,providerId:routed.selection.provider.id,modelId:routed.selection.model.id,durationMs});this.dependencies.logger.info('اكتمل طلب أوربي.',{requestId,organizationId:request.identity.organizationId,sessionId:session.id,providerId:routed.selection.provider.id,modelId:routed.selection.model.id,routingMode:decision.mode,routingIntent:decision.intent,routingComplexity:decision.complexity,durationMs});return {requestId,sessionId:session.id,text:normalized.text,providerId:routed.selection.provider.id,modelId:routed.selection.model.id,usage:normalized.usage,attempts:routed.attempts,durationMs};}catch(error){const normalized=normalizeOrbyError(error),durationMs=Date.now()-started;await this.dependencies.events.emit('request.failed',{requestId,organizationId:request.identity.organizationId,userId:request.identity.userId,sessionId:session?.id,errorCode:normalized.code,durationMs});this.dependencies.logger.error('فشل طلب أوربي.',{requestId,organizationId:request.identity.organizationId,sessionId:session?.id||null,errorCode:normalized.code,durationMs});throw normalized;}}
 async *stream(request:OrbyKernelRequest):AsyncIterable<OrbyKernelStreamEvent>{const requestId=request.requestId||randomUUID(),started=Date.now();let session:OrbySession|undefined;try{const prepared=await this.prepare(request,requestId);session=prepared.session;const streamCapabilities:OrbyProviderCapability[]=[...(request.requiredCapabilities||['text'])];if(!streamCapabilities.includes('streaming'))streamCapabilities.push('streaming');const decision=this.decide(request,prepared.configuration,streamCapabilities),preferredModelId=decision.preferredModelId||prepared.configuration.defaultModelId,baseRequest=providerRequest(requestId,preferredModelId||'auto',prepared.prompt.messages,prepared.configuration,request),selections=this.dependencies.routing.select({preferredModelId,allowedProviderIds:prepared.configuration.allowedProviderIds,allowedModelIds:prepared.configuration.allowedModelIds,requiredCapabilities:streamCapabilities},baseRequest),attempts:OrbyRoutingAttempt[]=[];let lastError:OrbyError|null=null,previous=selections[0],finalText='',finalUsage:OrbyKernelResponse['usage'];const maxAttempts=Math.max(1,prepared.configuration.maxAttempts);for(let attempt=1;attempt<=maxAttempts;attempt++){const selection=selections[(attempt-1)%selections.length];if(attempt>1&&(previous.provider.id!==selection.provider.id||previous.model.id!==selection.model.id))await this.dependencies.events.emit('provider.switched',{requestId,fromProviderId:previous.provider.id,toProviderId:selection.provider.id,fromModelId:previous.model.id,toModelId:selection.model.id});previous=selection;let emitted=false,finishReason:string|undefined;try{yield {type:'start',requestId,sessionId:session.id,providerId:selection.provider.id,modelId:selection.model.id};for await(const event of selection.provider.stream({...baseRequest,model:selection.model.providerModel})){if(event.type==='delta'){emitted=true;finalText+=event.text;yield {type:'delta',text:event.text};}else if(event.type==='usage'){finalUsage=event.usage;yield {type:'usage',usage:event.usage};}else if(event.type==='end')finishReason=event.finishReason;}const normalized=this.responseManager.normalize({text:finalText,usage:finalUsage,finishReason});attempts.push({providerId:selection.provider.id,modelId:selection.model.id,attempt,status:'succeeded'});const durationMs=Date.now()-started,response:OrbyKernelResponse={requestId,sessionId:session.id,text:normalized.text,providerId:selection.provider.id,modelId:selection.model.id,usage:normalized.usage,attempts,durationMs},routeMeta=routingMetadata(decision);await this.dependencies.sessions.append(session,[orbyMessage('user',request.message,request.metadata),orbyMessage('assistant',normalized.text,{providerId:selection.provider.id,modelId:selection.model.id,requestId,streamed:true,...routeMeta})]);await this.dependencies.events.emit('request.completed',{requestId,organizationId:request.identity.organizationId,userId:request.identity.userId,sessionId:session.id,providerId:selection.provider.id,modelId:selection.model.id,durationMs});this.dependencies.logger.info('اكتمل بث طلب أوربي.',{requestId,organizationId:request.identity.organizationId,sessionId:session.id,providerId:selection.provider.id,modelId:selection.model.id,routingMode:decision.mode,routingIntent:decision.intent,routingComplexity:decision.complexity,durationMs});yield {type:'end',response};return;}catch(error){lastError=normalizeOrbyError(error,'PROVIDER_UNAVAILABLE');attempts.push({providerId:selection.provider.id,modelId:selection.model.id,attempt,status:'failed',errorCode:lastError.code});await this.dependencies.events.emit('provider.failed',{requestId,providerId:selection.provider.id,modelId:selection.model.id,attempt,errorCode:lastError.code});if(emitted||!lastError.retryable||attempt===maxAttempts)break;finalText='';finalUsage=undefined;}}throw lastError||new OrbyError('تعذر بث استجابة أوربي.','PROVIDER_UNAVAILABLE',true);}catch(error){const normalized=normalizeOrbyError(error),durationMs=Date.now()-started;await this.dependencies.events.emit('request.failed',{requestId,organizationId:request.identity.organizationId,userId:request.identity.userId,sessionId:session?.id,errorCode:normalized.code,durationMs});throw normalized;}}
}
