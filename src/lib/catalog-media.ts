export function catalogImageUrl(value:unknown){const path=String(value||'').trim();if(!path)return null;if(/^https:\/\//i.test(path))return path;const base=process.env.NEXT_PUBLIC_SUPABASE_URL?.replace(/\/$/,'');if(!base)return null;const objectPath=path.startsWith('catalog-images/')?path.slice('catalog-images/'.length):path;return `${base}/storage/v1/object/public/catalog-images/${objectPath.split('/').map(encodeURIComponent).join('/')}`;}
